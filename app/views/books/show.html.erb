
<%= link_to "Back to Books", books_url %> | <%= link_to "Edit", edit_book_path(@book) %> | <%= link_to "Delete", @picture, method: :delete, data: { confirm: "Are you sure you want to delete this picture?" } %>
<p><%= @book.title %>, <%= @book.author %>, <%= @book.year %></p>
<%= @book.description %><br>
<%= image_tag(@book.url) %><br>

<%= render :partial => 'voting', :locals => {:book => @book } %>
